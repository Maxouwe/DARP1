CREATE TABLE brandqfidf(
    brand text,
    qfidf real,
    PRIMARY KEY(brand)
);

CREATE TABLE modelqfidf(
    model text,
    qfidf real,
    PRIMARY KEY(model)
);

CREATE TABLE typeqfidf(
    type text,
    qfidf real,
    PRIMARY KEY(type)
);

CREATE TABLE mpgqfidf(
    mpg real,
    qfidf real,
    PRIMARY KEY(mpg)
);

CREATE TABLE cylindersqfidf(
    cylinders integer,
    qfidf real,
    PRIMARY KEY(cylinders)
);

CREATE TABLE displacementqfidf(
    displacement real,
    qfidf real,
    PRIMARY KEY(displacement)
);

CREATE TABLE horsepowerqfidf(
    horsepower real,
    qfidf real,
    PRIMARY KEY(horsepower)
);

CREATE TABLE weightqfidf(
    weight real,
    qfidf real,
    PRIMARY KEY(weight)
);

CREATE TABLE accelerationqfidf(
    acceleration real,
    qfidf real,
    PRIMARY KEY(acceleration)
);

CREATE TABLE model_yearqfidf(
    model_year integer,
    qfidf real,
    PRIMARY KEY(model_year)
);

CREATE TABLE originqfidf(
    origin integer,
    qfidf real,
    PRIMARY KEY(origin)
);


INSERT INTO brandqfidf
SELECT brandqf.brand, brandqf.qf*brandidf.idf
FROM brandqf
INNER JOIN brandidf
ON brandqf.brand = brandidf.brand;